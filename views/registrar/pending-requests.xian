<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Requests - MinSU DocuReg</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @keyframes fadeInDown { from { opacity: 0; transform: translateY(-30px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
        .animate-fade-in-down { animation: fadeInDown 0.6s ease-out; }
        .animate-fade-in-up { animation: fadeInUp 0.6s ease-out forwards; opacity: 0; }
        .table-row-hover { transition: all 0.2s ease; }
        .table-row-hover:hover { background-color: #f3f4f6; transform: translateX(4px); }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-50 via-yellow-50 to-gray-100 min-h-screen">
    {{>navbar}}

    <div class="max-w-7xl mx-auto px-6 py-12 pt-24">
        <!-- Header -->
        <div class="animate-fade-in-down mb-10">
            <h1 class="text-5xl font-black text-gray-800 mb-2">Pending Requests Queue</h1>
            <p class="text-gray-600 text-lg">Review and process student document requests</p>
        </div>

        <!-- Requests Table -->
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden animate-fade-in-up">
            <div class="bg-gradient-to-r from-yellow-600 to-orange-600 px-8 py-6">
                <h2 class="text-white text-2xl font-black flex items-center"><i class="fas fa-tasks mr-3"></i> Pending Requests</h2>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-100 border-b-2 border-gray-300">
                        <tr>
                            <th class="px-8 py-4 text-left text-gray-800 font-bold">Student Name</th>
                            <th class="px-8 py-4 text-left text-gray-800 font-bold">Student ID</th>
                            <th class="px-8 py-4 text-left text-gray-800 font-bold">Document</th>
                            <th class="px-8 py-4 text-left text-gray-800 font-bold">Purpose</th>
                            <th class="px-8 py-4 text-left text-gray-800 font-bold">Date</th>
                            <th class="px-8 py-4 text-left text-gray-800 font-bold">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each requests}}
                            <tr class="table-row-hover border-b-2 border-gray-200">
                                <td class="px-8 py-5 text-gray-800 font-medium"><i class="fas fa-user mr-2 text-blue-600"></i>{{this.student.name}}</td>
                                <td class="px-8 py-5 text-gray-600 font-mono">{{this.student.studentId}}</td>
                                <td class="px-8 py-5">
                                    <span class="bg-blue-100 text-blue-800 px-4 py-2 rounded-full text-sm font-bold inline-flex items-center">
                                        <i class="fas fa-file mr-2"></i>{{this.documentType}}
                                    </span>
                                </td>
                                <td class="px-8 py-5 text-gray-600">{{truncate this.purpose 50}}</td>
                                <td class="px-8 py-5 text-gray-600 font-medium">{{formatDate this.createdAt}}</td>
                                <td class="px-8 py-5">
                                    <a href="/registrar/requests/{{this.id}}" class="inline-flex items-center bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-6 py-2 rounded-lg transition font-bold transform hover:scale-105 shadow-md">
                                        <i class="fas fa-arrow-right mr-2"></i> Process
                                    </a>
                                </td>
                            </tr>
                        {{else}}
                            <tr>
                                <td colspan="6" class="px-8 py-12 text-center text-gray-500">
                                    <i class="fas fa-check-circle text-5xl mb-4 block text-green-600 opacity-70"></i>
                                    <p class="text-lg font-bold">All requests processed!</p>
                                    <p class="text-sm mt-2">Great work! No pending requests.</p>
                                </td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    {{>footer}}
</body>
</html>
